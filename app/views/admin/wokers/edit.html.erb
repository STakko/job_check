<div>
  <h2>従業員編集</h2>
  <div>
    <%= form_with model:@woker, url:admin_woker_path, method: :patch, local:true do |f| %>
      <label for="title">従業員名</label>
      <%= f.text_field :name %>
      <%= f.submit ' 変更を保存 ' %>
    <% end %>
  </div>
</div>