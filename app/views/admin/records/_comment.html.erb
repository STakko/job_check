<div>
<% record.record_comments.each do |record_comment| %>
  <%= record_comment.record.woker %>
  <%= record_comment.comment %>
  <%= record_comment.created_at.strftime('%Y/%m/%d') %>
  <%= link_to "削除", admin_record_record_comment_path(record_comment.record, record), method: :delete %>
<% end %>
</div>

<div>
  
</div>